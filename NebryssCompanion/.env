# Local Development Environment
# API and WS URLs are injected dynamically at runtime by the server.
# Do NOT hardcode them here — they are auto-resolved from the request host.

MONGODB_DB_MAIN=Nebryss-assets
MONGODB_DB_PLAYERS=NebryssCampaignAssets

# Local MongoDB Community Server
MONGODB_URI=mongodb://127.0.0.1:27017/NebryssCompanion