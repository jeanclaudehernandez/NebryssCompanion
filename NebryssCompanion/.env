# Local Development Environment
# API and WS URLs are injected dynamically at runtime by the server.
# Do NOT hardcode them here — they are auto-resolved from the request host.

MONGODB_DB_MAIN=Nebryss-assets
MONGODB_DB_PLAYERS=Nebryss-players-local

# MongoDB Atlas (used if reachable, otherwise falls back to local JSON in src/assets/)
MONGODB_URI=mongodb+srv://jeanhernandezmeze_db_user:6hBfhByK45BGAxdy@cluster0.wjbet35.mongodb.net/